{
  "name": "six-mans-leaderboard",
  "version": "0.2",
  "homepage": "https://callum-c.github.io/Six-Mans-Leaderboard",
  "dependencies": {
    "axios": "^1.5.0",
    "node": "^20.6.0"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "npm install"
  }
}
